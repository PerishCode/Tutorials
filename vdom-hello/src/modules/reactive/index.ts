import {
  reactive,
  observe,
  unobserve,
  wrapFunctionAsDependency,
} from './engine'

import { WeakMapExtension, MapExtension } from './types'

export {
  reactive,
  observe,
  unobserve,
  wrapFunctionAsDependency,
  WeakMapExtension,
  MapExtension,
}
